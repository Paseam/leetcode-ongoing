set(BUBBLE_SRCS
    bubble_sort.h
)
add_executable(bubble_sort ${BUBBLE_SRCS})
#target_include_directories(bubble_sort )

set(EASY_SELECTION_SRCS
    easy_selection_srot.cpp
)
add_executable(easy_selection_sort ${EASY_SELECTION_SRCS})